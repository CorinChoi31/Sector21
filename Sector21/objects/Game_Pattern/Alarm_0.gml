if(variable_instance_exists(target_instance,"game_enemy_list"))
{
	//--> [wave, direction, id, density, rotate, range, color, health, damage, speed, accel, amount]
	
	ds_list_add(target_instance.game_enemy_list, 
		
	);
}

instance_destroy();